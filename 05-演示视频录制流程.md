# 05-演示视频录制流程（完整版）

> 目标：录一段 8-10 分钟、能完整讲清「企业外汇需求商机雷达」的演示视频。
> 原则：**先本地跑通一遍再开录**，视频里只出现演示需要的内容，隐藏真实姓名/内部资料。

---

## 〇、录制前准备（一次性）

| 项 | 说明 |
|----|------|
| 录屏工具 | 推荐 **OBS Studio**（免费，画质好、可录麦克风）；备选 Windows 自带 `Win+G`（Xbox Game Bar） |
| 分辨率 | 系统显示缩放建议 100%（或 125%），录屏分辨率 ≥1920x1080 |
| 窗口 | pi-web 与浏览器都**最大化**，录屏区域选"显示器"或"窗口" |
| 隐私 | 关闭微信/邮件通知；Pi 会话里不要出现真实姓名、导师姓名、内部系统截图 |
| 素材 | 先跑通一次全流程，确认队列有 ≥10 条、证据链接可点开 |
| 录音 | OBS 里选好麦克风，先录 10 秒试听 |

---

## 一、演示脚本（约 9 分钟）

### 1. 开场 + 背景（30 秒）
- 一句话：`工商银行深圳分行实习生 · 基于 Pi Coding Agent 的企业外汇需求商机雷达`
- 说清楚痛点：交易员以前靠经验自己搜"哪些公司可能有结售汇需求"，再手工查系统、上门营销。

### 2. 启动桌面端（30 秒）
```powershell
cd E:\pi-web\pi-web
npm start          # 生产模式，浏览器自动打开 http://127.0.0.1:30141
```
- 画面确认：左上角有红色 **ICBC** 标识 + "工商银行 · 企业外汇商机雷达"。

### 3. 选择项目目录（30 秒）
- 左侧栏 `Select project...` → 选 `基于picoding-agent的企业外汇需求商机雷达agent实现`。
- 解释：Pi 会在该项目下执行引擎脚本。

### 4. 一句话跑全流程（2-3 分钟，核心）
在输入框输入：
```
跑一遍企业外汇需求商机雷达
```
- Pi 会依次调用工具，**把镜头对准右侧输出**，逐个讲解：
  1. `crawl_cninfo` → 抓巨潮公告（搜"外汇套保/远期结售汇/套期保值"等关键词，45 天窗口）
  2. `sina_news` / `eastmoney_news` → 抓新浪、东方财富 7x24 快讯做舆情软信号
  3. `rule_screen` → 规则初筛：广东企业 + 外汇套保 + 近 45 天（硬规则），舆情走软信号评分
  4. `review_opportunity(all)` → DeepSeek 大模型复核：证据摘要/已知事实/待核实项/建议沟通问题
  5. `build_daily_queue` + `render_web` → 生成今日商机队列并渲染页面
- 每步让 Pi 打印完再继续（`/fx-daily` 也可一键执行）。

### 5. 展示商机队列页面（2 分钟）
```powershell
python engines\serve.py     # 演示服务，端口 8000
```
浏览器打开 `http://127.0.0.1:8000`：
- 左侧今日商机队列（12 条，按分排序）；点一家公司看右侧详情。
- **点「证据摘要」里的蓝色文档标题 /「查看原文」按钮**：
  - 巨潮公告 → 直接打开 PDF 原文；
  - 舆情线索（样例）→ 跳转百度搜索相关报道。
- 讲解评分五维：事件可信度、资金规模、时效性、覆盖度、信息完整度。

### 6. 认领 / 标记无效（30 秒）
- 点「认领商机 >」→ 生命周期变"待核实"，负责人=你；刷新后仍保留（持久化到 SQLite）。
- 说明：这对应交易员的线索管理动作。

### 7. 收尾（30 秒）
- 一句话总结：**从"人找信息"到"信息找人"**；展望：接行内 GC 台账校验、加企业财务字段（境外收入/汇兑损益）、每日定时推送。

---

## 二、如果中途翻车怎么办

| 现象 | 处理 |
|------|------|
| 巨潮抓取失败 | 引擎自动回退样例数据（source 标注"样例"），继续演示 |
| DeepSeek 复核太慢 | 等 60 秒内完成；也可先 `python engines\llm_review.py --all` 预热 |
| 舆情源无命中 | 自动回退样例舆情（标注"样例"），不影响流程展示 |
| 浏览器没弹出 | 手动打开 `http://127.0.0.1:30141` / `http://127.0.0.1:8000` |
| 端口被占用 | 换端口：`npm start -- -p 30142`、`python engines\serve.py --port 8001` |

---

## 三、录制要点提醒

- **先彩排 1 遍**：完整走通「跑一遍」+「打开页面」+「点证据」，再正式录。
- **多录 1-2 分钟空余**：开头留 3 秒静默，结尾留 2 秒再停，方便剪辑。
- **画面节奏**：Pi 输出过程快，录 0.5x 播放也不怕；讲解时语速放慢。
- **录完自查**：回放确认（1）ICBC 标识入镜（2）证据链接真的点开了（3）没有出现真实姓名。
- 导出建议：MP4 + H.264，1080p，比特率 ≥8Mbps。
