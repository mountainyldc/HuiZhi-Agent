# 新功能问题定义（产品经理版）

> **实施状态（2026-08-06）：本批 6 个功能已全部按本文档定义完成实现**，验收方式见 `07-汇报功能清单.md` 第 13-18 节。全局未定项在开发中按以下默认执行（如需调整可再改）：①一次全做；②话术/设置进看板按钮，其余进 Pi 对话；③记忆为单会话 SQLite 单行、无个人信息；④搜索后端 agent-reach/mcporter → so.com 零配置 → 降级提示；⑤设置仅关键词/排除词/时间窗口，下次运行生效。

> 范围：P0-4 拜访话术生成器 / P0-2 企业画像增强 / P0-1 跨轮次记忆 / P0-3 按需实时 Web 搜索 / P1-2 Plan 动态规划 / 雷达页「设置·规则过滤词」。
> 本文档只做问题定义，不含实现方案。**所有「未定」项需要业务方确认后才能进入开发。**

---

## P0-4 拜访话术生成器

- **最终交付物**：一个可调用的功能——输入商机（或公司名），输出一份**拜访话术**：30 秒电话开场白、上门拜访提纲、针对该公司币种/规模的产品建议、常见异议应对话术。入口至少包含 pi-web 对话；是否上雷达页面按钮**未定**。
- **硬约束**：所有具体数字/事实必须来自检索证据，无证据内容不写或明确标「需核实」；中文输出；缺 Key/断网时可降级为明确报错，不静默失败。
- **隐含要求**：话术要像客户经理说的话（口语、可照读），不是泛泛分析；电话与上门两种场景要分开；内容与商机详情（币种、额度、公告日期）一致。
- **什么叫完成**：输入「帮东鹏饮料生成拜访话术」→ 输出结构完整的开场白+提纲+产品建议+异议应对，且抽查每条关键数字都能在证据里找到出处。
- **最容易翻车**：LLM 编造客户情况或行内产品政策；话术空洞无针对性；用户不知道从哪里触发。

## P0-2 企业画像增强

- **最终交付物**：把现有「企业档案」升级为**拜访前必看画像卡**，新增字段：营收规模、出口占比/境外子公司、外汇敞口方向（收款/付款为主）、历史套保记录、建议切入产品。展示在看板详情页，并支持对话提问。
- **硬约束**：每个字段必须标注来源（公告/年报/舆情）与披露日期；**未披露字段标「未披露」，不允许用舆情/推断冒充事实**；画像与公告原文矛盾时以原文为准。
- **隐含要求**：画像卡「5 分钟能看完」；同一份画像要被 RAG 问答和话术生成复用（口径一致）。
- **什么叫完成**：队列 Top N（N 待定，默认 20）每家公司都有画像卡，页面可看、对话可问，缺失字段全部有明确标注。
- **最容易翻车**：把舆情当事实过度推断；字段口径不统一导致前后矛盾；画像信息过期（用了旧年报）。

## P0-1 跨轮次记忆

- **最终交付物**：pi-web 会话内跨轮次记忆——记住「分析过哪些公司、关注哪些区域/行业、已认领哪些商机」，后续提问自动带上相关上下文（如追问「那它的子公司呢」能理解指哪家公司）。
- **硬约束**：不存储任何客户个人信息；记忆必须**可查看、可清除**；记忆不改变无上下文问题的回答质量。
- **隐含要求**：用户在连续追问时感觉「被记住」；记忆错误时优先服从当前问题，不误导回答。
- **什么叫完成**：连续对话演示——分析东鹏→追问子公司/额度→系统正确指代；设置里有「清除记忆」入口；切换公司后不被旧记忆串扰。
- **最容易翻车**：记忆串扰（把 A 公司信息套到 B 公司）；跨天/跨会话边界不清；记忆膨胀拖慢回答。**记忆范围（单会话 vs 全局、保留多久）未定。**

## P0-3 按需实时 Web 搜索

- **最终交付物**：一个工具 `web_search`——用户问政策/竞品/行业动态等语料库里没有的时效信息时，实时联网检索，返回**带来源链接**的结果并纳入回答。
- **硬约束**：每条结果必须带来源 URL；网络信息在回答中标注「网络信息，需人工核验」，与公告/年报证据**分开标注**；搜索后端失败时明确降级提示。**后端选型未定（agent-reach / 搜索 API / 自建爬虫）。**
- **隐含要求**：用户能看出「这句话来自语料证据、那句来自实时网络」；金融政策解读措辞谨慎，不做投资建议。
- **什么叫完成**：问「最近外汇管理局有什么新政策」→ 返回带来源的实时结果并纳入回答；演示时断网也能优雅提示。
- **最容易翻车**：搜索结果质量差/时效差；演示现场网络不稳；对政策/行情的解读踩合规红线。

## P1-2 Plan 动态规划

- **最终交付物**：把固定 workflow 升级为「意图识别 → 动态工具链」——不同问法自动路由到对应工具组合（分析公司 / 跑全流程 / 生成话术 / 查画像 / 查队列）。
- **硬约束**：路由结果必须可解释（告知用户为什么走这条链路）；无法识别时兜底到「跑全流程」或普通问答；**不得破坏现有 `/radar` 命令与固定 workflow**。
- **隐含要求**：一句话触发复杂任务（如「准备明天拜访材料」自动组合档案+财报+RAG+话术）；演示要稳定可复现。
- **什么叫完成**：5 类典型问法（分析/跑流程/话术/画像/查队列）各自动走正确工具链，输出与手动逐工具调用一致，且路由错误率在演示中为 0。
- **最容易翻车**：意图误判跑错流程（浪费时间且演示尴尬）；LLM 路由不稳定；与现有 skill 冲突。**意图清单与路由规则（规则 vs LLM 判断）未定。**

## 雷达页「设置 · 规则过滤词」

- **最终交付物**：看板新增「设置」入口，客户经理可**配置规则过滤的关键词**（生效词/排除词），保存后影响后续抓取与初筛，并支持一键恢复默认。
- **硬约束**：配置持久化（重启不丢）；不破坏默认配置（有恢复默认入口）；页面操作无需改代码/重启服务。
- **隐含要求**：客户经理能自己调词，页面有直观的「当前生效关键词」展示；改词后能看到影响说明（如「词太宽可能增加噪声」）。
- **什么叫完成**：在页面增删关键词→保存→重跑流程按新词过滤且队列正常；排除词确实能把对应公司挡掉；一键恢复默认生效。
- **最容易翻车**：关键词配太宽/太窄导致队列异常或为空；与代码里写死的规则冲突（两套配置不一致）；保存失败用户不知道。**配置作用范围（仅关键词 vs 含评分权重/地区/时间窗口）与生效时机（保存即生效 vs 下次运行生效）未定。**

---

## 全局未定项（需确认后开工）

1. 话术/画像/记忆/搜索这 5 个功能**优先级与排期**（一次做完还是分两批）。
2. 各功能的**入口**：只放对话，还是也上雷达页面按钮/卡片。
3. 记忆的**持久化范围**与保留时长；画像的**覆盖公司数**。
4. 实时搜索的**后端选型**与合规边界。
5. 「设置」的**作用范围与生效时机**。

> 以上 5 项确认前，任何一条「未定」都可能影响验收口径，建议先对齐再开发。
