# 13-简历与仓库呈现策略

> 目的：把「工银汇智 · 企业外汇智能体」仓库（GitHub: https://github.com/mountainyldc/ICBC-HuiZhi-Agent）做成简历上能证明**自己深度参与**的作品，而不是"一看就是 AI 生成"的 demo。
> 原则：**不伪装成手写代码**，而是把真实的人类贡献（需求、架构、选型、踩坑、验收）显性化——面试官防的不是 AI 代写，而是"你不懂自己的项目"。

---

## 一、核心思路（三层）

| 层 | 目标 | 一句话做法 |
| --- | --- | --- |
| 仓库层 | 观感像工程而非 demo | 补测试/CI/文档/决策记录，README 有图有入口 |
| 简历层 | 写"负责了什么"而非"写了什么" | 突出需求调研、技术选型、迭代验收等角色产出 |
| 面试层 | 能讲 30 分钟 | 准备讲稿：为什么这么设计、数据从哪来、不准怎么办 |

> 判断标准：面试官深挖任何一个模块时，你能讲清「为什么存在、怎么实现、遇到什么问题、怎么权衡」——这比"是不是 AI 写的"重要得多。

---

## 二、仓库层改造清单

### 2.1 工程化（当前最缺，优先做）
- [x] **pytest 单元测试**：`tests/` 覆盖核心引擎
  - `settings.py`：关键词/排除词/时间窗口的读写与回退
  - `rule_screen.py`：硬规则、排除词、`days_window` 生效
  - `dispatch.py`：5 类意图路由
  - `retrieve.py`：FTS5 检索与 RRF 融合（可用样例数据断言）
  - `store.py`：画像/记忆的 upsert/get

✅ 已落地：26 个用例覆盖 settings / rule_screen / dispatch / retrieve / store，CI 每次 push / PR 自动运行。
现在项目是"跑一次看结果"，出问题只能人肉试。单元测试是把每个模块的输入输出写成可自动检查的用例（比如"排除词填'拓邦股份'，队列里必须没有拓邦"），以后改代码一跑就知道有没有改坏。
面试价值：能说"我有 XX 个测试用例，CI 每次自动跑"——这是专业开发者的标配动作。

- [x] **GitHub Actions CI**：`.github/workflows/ci.yml`
  - `py_compile` 全部引擎 + `pytest` + （可选）`npx tsc --noEmit`
  - 触发：push / PR 到 main
免费帮你干活的机器人：每次你把代码推到 GitHub，它自动在云端跑一遍"编译 + 测试"，红了就警告你。
面试价值：面试官打开仓库看到绿色 ✓ 徽章，第一印象直接是"这是正经项目"。

- [x] **CHANGELOG.md**：按日期记录功能与修复（已发生的 08-04/08-05/08-06 可补写）
按日期记"这版加了什么、修了什么"。现在你的更新散在 README 里，CHANGELOG 是行业惯例，一眼看出迭代轨迹（08-04 初版 → 08-05 看板/RAG → 08-06 六个新功能），这也是"深度参与"的时间线证据。

- [x] **LICENSE**：默认 MIT（开源简历最常见）；确认后再添加
声明别人能用你的代码的许可。不加 LICENSE，严格说别人不能合法使用你的开源仓库，专业项目都会加。
面试价值：提到"我开源了项目，用 MIT 协议"是加分细节。

- [x] **CONTRIBUTING.md**：环境搭建、跑测命令、提交规范（短说明写"为什么"）

写给想参与的人看的"贡献指南"：怎么装环境、怎么跑测试、提交代码有什么规范。即使没人真来贡献，它传达的是"我按规范做项目"。

- [x] **issue / PR 模板**：`.github/ISSUE_TEMPLATE/`、`PULL_REQUEST_TEMPLATE.md`
  别人提 bug、提需求时自动弹出的固定格式（要写清楚背景/复现步骤/预期）。让仓库看起来是"被管理的项目"而非"个人杂物间"。

- [x] `.gitignore` 复核：确保 `data/db/`、`data/queue_snapshots/`、PDF 不入库（clone 即用靠自动重建）
确保数据库、临时文件、PDF 缓存等不该进 Git 的东西不会误传上去。你项目里 data/db/、data/queue_snapshots/ 已经在忽略列表，复核一下就行。

### 2.2 文档（体现"人的判断"）
- [x] **架构决策记录（ADR）**：已落盘 `adr/`（14 条 + 索引 README.md），每个决策写：背景 / 决策 / 后果（收益+代价）/ 何时重估，面试直接引用编号
  - ADR-0001 方案选型：Pi Agent 编排 + 规则确定性兜底（而非自研 UI/纯爬虫）
  - ADR-0002 两段式筛选：规则初筛 + 5 维可解释评分 → LLM 复核（不用纯 LLM 端到端）
  - ADR-0003 SQLite 单文件存储（不引 MySQL/Postgres）
  - ADR-0004 抓取失败降级链：重试 → 样例回退 → source 标注
  - ADR-0005 静态 HTML + serve.py 轻量服务（不引前后端框架）
  - ADR-0006 Pi 扩展只做桥接，业务逻辑放 Python 引擎（白名单安全）
  - ADR-0007 RAG 混合检索：FTS5 BM25 + 向量 + RRF（不引 Milvus/重排模型）
  - ADR-0008 faiss 中文路径问题（Windows fopen ANSI）ASCII 临时路径绕行
  - ADR-0009 Embedding 用百炼 text-embedding-v4，无 key 降级 BM25
  - ADR-0010 画像口径：未披露必标 + 每字段来源/披露日期 + 以公告原文为准
  - ADR-0011 记忆边界：只存公司/区域名、可查看可清除、无个人信息
  - ADR-0012 实时搜索三级后端链：Exa → so.com → 降级提示
  - ADR-0013 意图路由用规则判断（非 LLM），输出可解释原因
  - ADR-0014 设置持久化 settings.json + 下次运行生效 + 一键恢复默认
- [ ] **README 强化**：
  - 架构图（mermaid：抓取 → 初筛 → 复核 → 队列 → RAG/画像/话术）
  - 看板/画像卡/话术输出截图
  - 显式列出「需求与设计文档」入口（00-12，体现产品思维）
  - 目录放好：快速开始、架构、功能清单、接口、边界与降级
- [ ] 保留现有 00-12 编号文档 = 最强的"人类参与证据"（问题定义、方案对比、会议纪要）

### 2.3 提交历史
- [ ] **不重写历史**（重写看起来像心虚，且 git history rewrite 是危险信号）
- [ ] 以后自己提交，格式：`类型(模块): 一句话说明为什么改`
  - 例：`fix: 巨潮接口超时导致流程中断，加重试+样例兜底`
  - 例：`test: rule_screen 排除词命中公司名用例`
- [ ] 提交粒度：一次一个逻辑改动，避免超大提交

---

## 三、简历层写法

### 3.1 角色定位（示例 bullets，替换"开发了XX"）
```
中国工商银行（深圳分行） · 科技类实习生 2026.07 – 2026.08
- 完成金融市场部外汇业务需求调研：访谈交易员，梳理"结售汇商机发现"6 大痛点并落成问题定义
- 调研整合 9 类公开数据源（巨潮/东财/同花顺/新浪/港交所/商务部等），制定抓取与规则初筛逻辑
- 完成技术选型与系统骨架设计：基于 Pi Coding Agent 编排 17 个工具 + RAG 混合检索（BM25+向量+RRF）
- 主导功能验收与迭代：演示数据从 12 条扩到 100+，落地企业画像卡、拜访话术、设置项等功能
- 产出可复现交付：单仓库 clone 即用 + 桌面端 pi-web，配置 API Key 即可运行
```

### 3.2 红线
- [ ] 不声称"独立手写全部代码"——被深挖必露馅
- [ ] 不删行内内部资料后造假来源；仓库内只放公开信息（现状已做到：docs/ 不入库）
- [ ] 简历可写"AI 辅助提效"，重点放在你把关的方向与决策

---

## 四、面试层准备

### 4.1 30 分钟讲稿结构
1. 背景与问题（2 分钟）：交易员现状、6 大痛点、为什么不是量化交易
2. 我的角色（3 分钟）：需求调研 → 数据源整合 → 技术选型 → 骨架设计 → 验收迭代
3. 系统怎么跑（10 分钟）：数据流 + 一次 /radar 全流程 + RAG 问答 + 画像/话术
4. 关键技术决策（8 分钟）：方案 B 对比、混合检索、降级链、边界处理
5. 踩坑与教训（5 分钟）：巨潮超时、faiss 中文路径、数据量从 12→100+ 的过程
6. 后续方向（2 分钟）：结构化查询、Reflection 自评、日报订阅

### 4.2 被问"这是 AI 写的吧"的标准应答
> 框架：承认工具 → 立即转向人做的部分 → 举 1-2 个具体决策/踩坑。
>
> 示例："编码环节用了 AI 辅助提效，但方向、需求、架构、验收都是我主导的。比如导师提出'能不能从财报提取外汇收入作为业务规模依据'，我把它落成了企业画像卡——每个字段必须带来源和披露日期、未披露标'未披露'，防止舆情冒充事实；再比如搜索后端我做了三级降级链，演示断网也能优雅提示。要不要我演示一下数据流？"

### 4.3 必须能答的问题
- 为什么选 Pi Agent 而不是自研平台？（成本、可审计、内网部署、扩展机制）
- 规则初筛的硬规则和 5 维评分怎么定的？
- RAG 混合检索为什么用 FTS5 + 向量 + RRF？（中文子串匹配 + 语义 + 免调参融合）
- 数据不准/接口挂了怎么办？（重试、样例回退、来源标注、降级提示）
- 画像和记忆的边界为什么这么设计？（未披露必标、不存个人信息、可清除）

---

## 五、待确认 / 待办

- [ ] LICENSE 是否用 MIT（确认后添加）
- [ ] 是否给仓库加 topics（`fx-radar`、`rag`、`agent`、`fintech` 等）与主页链接
- [ ] 是否补截图到 README（看板 / 画像卡 / 话术输出）
- [ ] 是否开始补 pytest + CI（我可以直接实施，改动都推仓库）

> 原则再强调一遍：**真实参与是事实，AI 只是键盘**。把仓库的工程化和文档做起来，面试时讲清楚，就是最好的"深度参与证明"。