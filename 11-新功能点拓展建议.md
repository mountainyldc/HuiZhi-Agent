# 新功能点拓展建议
## ——对照“工业级 Multi-Agent”架构，工银汇智还能加什么

> 参考：企业培训 Agent · 多模态 Multi-Agent 系统（RAG 问答 / 实时 Web 搜索 / 智能 Agent 三模块 + Plan / Memory / Action / Tool / Reflection 五环节）
> 原则：优先选**与现有演示衔接自然、复杂度可控、汇报亮点足**的功能，稳定交付优先。

---

## 一、能力对照：参考项目 vs 工银汇智现状

| 参考项目能力 | 工银汇智现状 | 差距 |
|---|---|---|
| RAG 问答（混合检索） | ✅ 已有：FTS5 BM25 + 百炼向量 + RRF，证据级引用 | 基本对齐 |
| 多源实时抓取 | ✅ 已有：9 类数据源定时批量抓取 | 缺“**按需**实时 Web 搜索” |
| 工具编排（Tool） | ✅ 已有：Pi 扩展 11 个工具 + workflow | 缺“**按意图动态规划**” |
| 跨轮次记忆（Memory） | ❌ 无：每次提问上下文独立 | **新增点** |
| 用户画像 | ❌ 无（仅有企业档案 8 字段） | **新增点** |
| 个性化话术生成 | ❌ 无（早期原型提过，未落地） | **新增点** |
| 反思/重规划（Reflection） | ❌ 无：回答后不自我评估 | **新增点** |
| 多模态 | ❌ 无（年报 PDF 仅文本提取） | 增强点 |

---

## 二、建议新增功能点

### P0（与现有 RAG/对话直接衔接，演示最自然）

**P0-1 跨轮次对话记忆（Memory）**
- 做什么：在 pi-web 会话内记住“分析过哪些公司、用户关注哪些区域/行业、已认领哪些商机”，后续提问自动带上上下文（如：再问“那它的子公司呢”能理解指东鹏饮料）。
- 实现：SQLite `conversation_memory` 表（session_id / company / region / intent / summary），每次工具调用前后读写；Pi 扩展里注入历史摘要。
- 复杂度：低-中。演示价值：高（体现“智能体”而非“单次问答”）。

**P0-2 企业画像增强（用户画像落地到企业）**
- 做什么：现有企业档案只有法人/注册地/注册资本，扩展为“**拜访前必看画像卡**”：营收规模、出口占比/境外子公司（从公告时间线提取）、外汇敞口方向（收款/付款为主）、历史套保记录（历年套保公告）、建议切入产品。
- 实现：在 `build_profiles.py` / RAG 上叠加“画像生成器”（聚合公告 + 年报 + 已检索引擎，DeepSeek 输出结构化画像）。
- 复杂度：中。演示价值：高（导师关注的“业务规模预估”的自然延伸）。

**P0-3 按需实时 Web 搜索（突破语料截止）**
- 做什么：新增工具 `web_search`（可复用 agent-reach / 搜索 API）：用户问“最近外汇管理局有什么新政策”“XX 行业出口形势”时实时联网，返回带来源的结果并进入回答上下文。
- 为什么：现有语料止于公告/资讯库，政策动态、竞品、口碑是客户经理高频需求。
- 复杂度：中（选一个稳定搜索后端即可）。演示价值：高（“实时”是亮点）。

**P0-4 拜访话术生成器（Action 产出）**
- 做什么：新增工具 `generate_visit_pitch`：输入商机（或公司名）→ 输出 **30 秒电话开场白 + 上门拜访提纲 + 针对币种/规模的产品建议 + 应对话术**。
- 实现：复用 `analyze_company` 的证据检索结果，DeepSeek 按固定模板生成；早期原型已有 `generate_visit_brief` 思路，直接正式落地。
- 复杂度：低（模板 + 现有检索）。演示价值：极高（客户经理最想要的产出）。

### P1（体现“工业级”，工程含金量）

**P1-1 Reflection 反思自评**
- 做什么：RAG 回答生成后自评：证据覆盖是否充分、置信度打分；若置信度低，自动追加“需进一步核实”清单并建议补充检索词（与现有“需进一步核实”联动）。
- 实现：`rag_answer.py` 增加反思步骤（二次 LLM 调用或规则化自评）。
- 复杂度：中。演示价值：高（“AI 知道自己在哪没把握”）。

**P1-2 Plan 按意图动态规划**
- 做什么：把 workflow 从“固定顺序”升级为“意图识别 → 动态工具链”：说“分析比亚迪”→ 直接 `analyze_company`；“今天有什么新商机”→ 走抓取+初筛+队列；“准备明天拜访材料”→ 自动组合 档案 + 财报 + RAG + 话术生成。
- 实现：Pi 侧用 skill 规则 + LLM 判断意图路由，或新增 `plan_tools` 工具。
- 复杂度：中-高。演示价值：高（“一句话干复杂事”）。

**P1-3 商机日报自动订阅（Action 闭环）**
- 做什么：每日自动生成“今日商机日报”（TOP10 + 新增 + 待跟进已认领），推送到飞书/邮件。
- 实现：复用 github-weekly-digest 的飞书发布能力，加定时器。
- 复杂度：中。演示价值：中-高（“每天 8:30 自动出日报”很打动业务方）。

### P2（锦上添花）

**P2-1 多模态增强**：年报 PDF 表格版面解析（跨页合并）、上传公告/截图问“这段怎么解读”。
**P2-2 团队协作视图**：看板“我的商机”面板 + 跟进记录 + 下次跟进提醒。

---

## 三、五环节架构落地映射

| 环节 | 现有基础 | 建议新增 |
|---|---|---|
| Plan | workflow 固定顺序 | P1-2 意图路由/动态工具链 |
| Memory | 无 | P0-1 会话记忆 + 偏好 |
| Action | 11 个工具 | P0-4 话术生成、P1-3 日报推送 |
| Tool | Pi 扩展注册 | P0-3 web_search、P0-2 画像工具 |
| Reflection | 无 | P1-1 自评与降级提示 |

---

## 四、推荐路线（按汇报收益排序）

1. **先做 P0-4 话术生成 + P0-2 画像增强**：直接给客户经理“拜访前一套材料”，演示最直观，且完全复用现有检索；
2. **再做 P0-1 会话记忆 + P0-3 实时搜索**：补齐“智能体感”与“实时性”两块短板；
3. **有余力做 P1-1 反思 + P1-2 动态规划**：这两个是“工业级”叙事的关键词，放在 PPT 技术亮点页。

> 不推荐一上来做 P2 多模态/团队协作：投入大、与当前汇报主线（RAG + Agent 编排）关联弱。

---

## 五、风险与边界

- 实时 Web 搜索需选稳定后端，注意演示时网络与内容合规（标注来源、不采信未经核实信息）；
- 会话记忆注意隐私边界（只存公司/意图摘要，不存客户个人信息）；
- 所有新功能保持“可降级”：缺 Key/断网时自动跳过，不影响主流程。

