# 工银汇智 · 企业外汇智能体 —— 功能详细解释

> 本文档对 12 项核心功能逐项展开：**解决的问题 / 实现原理 / 涉及模块 / 使用方法 / 演示要点**，与《09-实习汇报报告》配套使用。
> 代码仓库：https://github.com/mountainyldc/ICBC-HuiZhi-Agent

---

## 功能 1：多数据源情报感知

**解决的问题**：交易员手动刷 Wind/网页找“走出去”企业，效率低、易漏。需要一个自动、稳定、可标注来源的资讯入口。

**实现原理**：
- 抓取引擎按来源拆分，互不阻塞：`crawl_cninfo`（巨潮公告）、`sina_news` / `eastmoney_news`（财经快讯）、`crawl_em_feed` / `crawl_ths` / `crawl_hkex` / `crawl_mofcom` / `crawl_gov_gd`（资讯中心各源）；
- 巨潮抓取按“外汇套保、远期结售汇、汇率避险、外汇衍生品、套期保值、汇兑、结售汇、跨境资金、外币”等关键词 + 时间窗口过滤；
- **失败兜底**：抓取异常自动回退本地样例数据，并在页面标注 `source=cninfo|sample`，演示不断链。

**涉及模块**：`engines/crawl_cninfo.py`、`sina_news.py`、`eastmoney_news.py`、`crawl_em_feed.py`、`crawl_ths.py`、`crawl_hkex.py`、`crawl_mofcom.py`、`crawl_gov_gd.py`。

**使用方法**：`python engines/crawl_cninfo.py`（单源）；或在 Pi 中说“抓取巨潮公告”。

**演示要点**：展示公告数量、来源标注（真实/样例）；资讯中心展示多来源聚合。

## 功能 2：规则初筛 + 5 维商机评分

**解决的问题**：抓回来的几千条公告里，哪些才是“广东企业 + 外汇套保需求”的真商机？

**实现原理**：
- **硬规则**（公告）：① 广东企业（`region_allowlist.csv` 名单）；② 标题命中外汇/套保关键词；③ 近 45 天官方公告；
- **软信号**（舆情）：财经快讯命中“广东企业 + 外汇/汇率/出口/海外/跨境”即产出线索，可信度较低，评分 65 分起，供人工参考；
- **5 维评分**（0-100，权重可配）：事件可信度 0.25 / 资金体量 0.20 / 时效性 0.20 / 我行覆盖度 0.20（当前为占位值，待接行内数据）/ 信息完整度 0.15。

**涉及模块**：`engines/rule_screen.py`、`engines/region_allowlist.csv`、`config.yaml`（score.weights）。

**使用方法**：`python engines/rule_screen.py --reset`；或 Pi 中调用 `rule_screen`。

**演示要点**：展示命中列表与每条分数；说明权重可配置；覆盖度维度标注“待接行内数据”，体现诚实边界。

## 功能 3：DeepSeek 大模型复核

**解决的问题**：规则初筛只看关键词，可能把“仅为披露套保制度”的公告误判为商机。需要语义级复核。

**实现原理**：
- 对初筛结果逐条调用 DeepSeek，输出：**证据摘要 / 已知事实 / 未知事实（需向客户核实项）/ 建议沟通问题 / 复核分**；
- 复核分与规则分结合决定最终排序；
- **降级设计**：未配置 `DEEPSEEK_API_KEY` 或接口超时时自动跳过，评分保持规则分，流程不中断。

**涉及模块**：`engines/llm_review.py`、`config.yaml`（review）。

**使用方法**：`python engines/llm_review.py --all`；或 Pi 中调用 `review_opportunity`（`all=true`）。

**演示要点**：展示一条商机的“已知/未知事实”与“建议沟通问题”——这是给客户经理最实用的产出。

## 功能 4：每日商机队列 + 潜在业务推断

**解决的问题**：客户经理需要一张“今天该去聊谁、聊什么”的清单，而不是一堆公告。

**实现原理**：
- `build_queue` 从 SQLite 按评分排序生成当日队列快照（公司/城市/分数/标签/触发事件/判断依据/原文链接）；
- 自动推断**潜在业务**：汇率避险 / 对外付款 / 对外收款 / 跨境结算（依据公告关键词与场景匹配）；
- **生命周期管理**：new（新发现）→ verifying（待核实，认领后）→ contacted（已联系）；另有 invalid（无效），支持认领与标记无效，状态持久化到 SQLite。

**涉及模块**：`engines/build_queue.py`、`engines/actions.py`、`engines/store.py`。

**使用方法**：`python engines/build_queue.py`；或在看板/对话中“认领/标记无效”。

**演示要点**：展示 118 条队列的排序、区域分布；认领一条 → 状态变“待核实”。

## 功能 5：年报财务引擎

**解决的问题**：导师提出“上市公司财报里会写清楚外汇收入，可作为预估业务规模大小的依据”。

**实现原理**：
- 从巨潮下载目标公司年报/半年报 PDF，解析公司信息块与财务数据；
- 提取**汇兑损益**等外汇相关指标，作为该企业外汇业务规模的初步预估依据，展示在商机详情页；
- 支持一键“更新”实时重抓（`/financials/update` 接口，force 模式）。

**涉及模块**：`engines/fetch_financials.py`、`engines/build_profiles.py`（共用 PDF 解析）。

**使用方法**：看板详情页点“更新”；或 `python engines/fetch_financials.py --company 东鹏饮料`。

**演示要点**：东鹏饮料详情页展示“最新年报数据”卡片，说明“汇兑损益 → 业务规模预估”的逻辑。

## 功能 6：可视化看板（双视图，苹果风 UI）

**解决的问题**：把队列数据变成客户经理能直接用的界面。

**实现原理**：
- **商机雷达视图**：左侧今日队列（评分排序、选中高亮），右侧详情（企业档案 / 触发事件 / 潜在业务判断 / 最新年报数据 / 建议沟通重点 / 判断依据 / 评分），证据链接可点击；
- **资讯中心视图**：公告 + 舆情聚合检索，支持关键词搜索、来源标签筛选、时间窗口、分页，一键“更新数据”重跑爬虫 + 筛选 + 渲染；
- 渲染为**自包含静态 HTML**（零依赖可双击打开）；认领/标记无效通过 `serve.py` 持久化到 SQLite；
- 米白暖色 Claude 风配色 + ICBC 品牌红，保持专业与辨识度。

**涉及模块**：`engines/web_template.html`、`engines/render_web.py`、`engines/serve.py`。

**使用方法**：`python engines/serve.py` → 打开 http://127.0.0.1:8000。

**演示要点**：队列 → 点击东鹏饮料 → 详情各卡片逐项展示；切“资讯中心”搜索关键词。

## 功能 7：Pi Agent 对话式编排

**解决的问题**：把整条流水线变成“说一句话就能跑”，而不是记一堆命令。

**实现原理**：
- 用 Pi Coding Agent **扩展（TypeScript）**注册 11 个工具：`crawl_cninfo / crawl_news / rule_screen / review_opportunity / build_daily_queue / render_web / start_server / analyze_company / ask_insights / claim_opportunity / mark_invalid`；
- **Skill 封装业务知识**（结售汇概念、商机识别规则、5 维评分、生命周期）与 **workflow 流程编排**（固定步骤顺序）；
- 用户输入“跑一遍企业外汇需求商机雷达”或 `/radar`，Pi 自动按序调用工具，聊天窗口实时展示每步执行结果。

**涉及模块**：`extensions/icbc-huizhi-agent.ts`、`skills/icbc-huizhi-agent/SKILL.md`、`skills/workflow/SKILL.md`。

**使用方法**：`pi -e ./extensions/icbc-huizhi-agent.ts` 后对话触发；全局安装见 README。

**演示要点**：一句话触发 → 展示 11 个工具依次执行（抓取 → 初筛 → 复核 → 队列 → 渲染 → 服务）。

## 功能 8：桌面端 pi-web（clone 即用）

**解决的问题**：终端界面不好看、汇报演示不专业；需要开箱即用的桌面级界面。

**实现原理**：
- 基于 Next.js 的 pi-web 前端，定制 **ICBC 工银汇智品牌**（顶栏红色 ICBC 标识、标题、窗口标题、HUIZHI 1.0）；
- 内置中文快速开始与 DeepSeek 配置教程；
- 与引擎仓库同仓发布：clone → `npm install && npm run build && npm start` → 配置 API Key 即用。

**涉及模块**：`../pi-web`（app / components / globals.css 等）。

**使用方法**：`cd pi-web && npm install && npm run build && npm start` → http://127.0.0.1:30141。

**演示要点**：展示桌面端聊天、模型配置、触发全流程；录演示视频用桌面端更专业。

## 功能 9：企业档案自动解析

**解决的问题**：客户经理拜访前要查“这家公司法人是谁、注册地、注册资本”，人工搜几小时。

**实现原理**：
- 自动下载 Top 公司年报/半年报 PDF，解析“公司信息”块；
- 输出 8 个字段：**法定代表人 / 注册地址 / 办公地址 / 邮编 / 网址 / 邮箱 / 股票代码 / 注册资本**；
- 数据校验后写入 `company_profiles` 表，看板商机详情页直接展示“企业档案”卡片，标注年报来源与披露日期。

**涉及模块**：`engines/build_profiles.py`、`engines/fetch_financials.py`、`engines/store.py`。

**使用方法**：`python engines/build_profiles.py --top 30`（可 `--company` 指定单家）。

**演示要点**：东鹏饮料档案卡（法人林木勤、深圳南山区注册地址、605499 等），说明“年报可信来源”。

## 功能 10：RAG 语料构建（公告正文摄入）

**解决的问题**：此前只存公告标题 + URL，深度分析没有正文可读，RAG 缺料。

**实现原理**：
- `ingest_docs` 对队列 Top 公司公告 PDF 自动下载并提取正文，写入 `documents` 表；
- 保留完整元数据：公告 / 公司 / 日期 / 来源 / 原文链接，证据可溯源；
- 当前语料：**30 篇公告正文**（Top 队列覆盖，含东鹏饮料、海大集团、劲拓股份等）。

**涉及模块**：`engines/ingest_docs.py`、`engines/store.py`（documents / doc_chunks 表）。

**使用方法**：`python engines/ingest_docs.py --top 30`。

**演示要点**：展示 documents 表数量与“标题 + URL + 正文”结构，说明这是 RAG 的数据底座。

## 功能 11：混合检索引擎（BM25 + 向量 + RRF）

**解决的问题**：单一关键词检索对“语义相近”问题不敏感；单一向量检索对精确关键词命中差。

**实现原理**：
- **关键词路**：SQLite FTS5（trigram tokenizer）实现中文子串匹配，零外部依赖；
- **语义路**：百炼 `text-embedding-v4`（1024 维）对分块（800 字/块、120 字重叠）编码，FAISS `IndexFlatIP` 内积检索；
- **融合**：两路 Top10 按 **RRF（k=60）** 融合取 Top5；问题中识别出公司名时自动限定该公司范围；
- 中文路径下 FAISS 读写封装为 ASCII 临时路径中转，避免兼容性问题。

**涉及模块**：`engines/retrieve.py`、`engines/index_docs.py`、`data/rag/faiss.index`。

**使用方法**：`python engines/retrieve.py --query "外汇套保" --top 5`。

**演示要点**：同一条问题分别展示关键词命中与语义命中；对“外汇衍生品交易”这类词验证双路互补。

## 功能 12：证据级智能问答（RAG 生成）

**解决的问题**：客户经理问“帮我分析东鹏饮料的外汇需求”，需要带依据、可验证的分析报告。

**实现原理**：
- 提问 → `retrieve` 混合检索证据 → 证据按 `[来源N]` 编号拼入 Prompt → DeepSeek 生成带引用回答；
- **强制引用**：提示词要求每条关键依据/数字/结论必须用 `[来源N]` 标注，禁止“详见原文链接”类占位文字；
- **一键溯源**：生成后自动把 `[来源N]` 替换为指向巨潮原文 PDF 的链接；若回答完全没有标记，自动在结尾追加可点击的“数据来源”列表，保证任何时候都能点开原文；
- Pi 新增 2 个工具：`analyze_company`（单公司深度分析）/ `ask_insights`（自由提问）。

**涉及模块**：`engines/rag_answer.py`、`engines/retrieve.py`、`extensions/icbc-huizhi-agent.ts`。

**使用方法**：`python engines/rag_answer.py --query "帮我分析东鹏饮料的外汇需求"`；或在 pi-web 直接对话。

**演示要点**：展示“结论 + 5 条依据 + 拜访建议”，点击 `[来源1]` 打开巨潮 PDF 验证——这是“AI 可信”到“AI 可验证”的关键演示。

---

## 附录 A：主链路时序

```
用户(pi-web)  →  "跑一遍企业外汇需求商机雷达"
   → Pi 按 workflow 依次调用：
   crawl_cninfo(公告) / crawl_news(舆情)
   → rule_screen(硬规则+软信号+5维评分)
   → review_opportunity(DeepSeek 复核)
   → build_daily_queue(队列快照)
   → render_web(看板 HTML)
   → start_server(预览 http://127.0.0.1:8000)
用户  →  看板认领/标记无效 → build_queue + render_web 刷新
用户  →  "分析东鹏饮料的外汇需求"
   → analyze_company → retrieve(混合检索) → rag_answer(DeepSeek, [来源N] 可点击)
```

## 附录 B：核心数据表（SQLite `fx_radar.db`）

| 表 | 用途 | 关键字段 |
|---|---|---|
| announcements | 公告/舆情原始数据 | title / company / publish_date / source / url |
| opportunities | 商机（评分/生命周期） | company_name / score / lifecycle / tags / evidence |
| documents | 公告正文语料 | title / url / raw_text / company / publish_date |
| doc_chunks | 分块（供向量/FTS 检索） | doc_id / chunk_text / chunk_index |
| company_profiles | 企业档案 | legal_rep / registered_address / registered_capital 等 |
| chunks_fts | FTS5 trigram 索引 | 由 doc_chunks 同步 |

## 附录 C：配置说明（`config.yaml`）

- `crawl`：时间窗口（默认 45/180 天）、关键词表、样例兜底开关；
- `score.weights`：5 维评分权重（合计 1.0）；
- `review` / `rag.llm`：DeepSeek 地址与模型（`api_key_env: DEEPSEEK_API_KEY`）；
- `rag.embedding`：百炼地址与模型（`api_key_env: DASHSCOPE_API_KEY`，1024 维）。

> 所有 API Key 一律从环境变量读取，不写死在代码/仓库中。
