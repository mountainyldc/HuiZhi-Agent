# 04-系统骨架：企业外汇需求商机雷达（方案 B · Pi Agent 编排）

> 更新日期：2026-08-04
> 定位：本文件只给"足够支撑编码"的骨架；不展开实现细节。
> 标记：【待定】= 需要确认；【边界不稳】= 本期按降级方案处理，先标注。

---

## 1. 模块拆分

| 模块                                  | 类型         | 一句话职责                           |
| ----------------------------------- | ---------- | ------------------------------- |
| Pi 扩展 `fx-lead-radar.ts`            | TypeScript | 向 Pi 注册 7 个工具，桥接 Pi 与 Python 引擎 |
| Skills `fx-lead-radar` / `workflow` | Markdown   | 业务知识 + 工具用法；"一句话跑全流程"的编排指令      |
| `crawl_cninfo.py`                   | Python 引擎  | 抓取巨潮公告，归一化为统一 JSON；失败回退样例       |
| `rule_screen.py`                    | Python 引擎  | 规则初筛（广东+套保+45天）+ 5 维评分，输出商机     |
| `llm_review.py`                     | Python 引擎  | 大模型复核：证据摘要/已知未知事实/建议沟通问题/复核分    |
| `build_queue.py`                    | Python 引擎  | 从库中生成"今日商机队列"快照                 |
| `render_web.py`                     | Python 引擎  | 由队列快照生成静态 HTML（队列+详情+认领/无效表单）   |
| `store.py`                          | Python 库   | SQLite 访问层：公告/商机/状态/线索          |
| `config.yaml`                       | 配置         | 地区、关键词、45天窗口、评分权重、LLM 配置        |

**编排关系**：Pi Agent 是主流程指挥者，按 workflow skill 依次调用扩展工具；工具内部调用 Python 引擎；引擎只读写 `data/`。

---

## 2. 目录结构

```
fx-lead-radar/
├── extensions/
│   └── fx-lead-radar.ts          # Pi 扩展：注册 7 个工具
├── skills/
│   ├── fx-lead-radar/SKILL.md    # 业务知识 + 工具用法说明
│   └── workflow/SKILL.md         # 全流程编排指令（Pi 现场执行）
├── engines/
│   ├── crawl_cninfo.py
│   ├── rule_screen.py
│   ├── llm_review.py
│   ├── build_queue.py
│   ├── render_web.py
│   └── store.py
├── data/
│   ├── db/fx_radar.db            # SQLite
│   ├── crawled/                  # 抓取原始公告 JSON
│   ├── queue_snapshots/          # 每日队列 JSON 快照
│   └── sample/                   # 样例公告（兜底）
├── web/
│   └── index.html                # render_web 输出（演示打开）
├── config.yaml
└── requirements.txt
```

---

## 3. 各模块职责

- **Pi 扩展**：只做参数校验、转义、调用引擎、返回 JSON；不含业务逻辑
- **workflow skill**：定义步骤顺序与失败降级（抓取失败→用样例；复核失败→跳过）
- **crawl_cninfo**：按关键词+日期窗口抓巨潮公告 → `data/crawled/*.json`
- **rule_screen**：读公告 → 规则命中（广东企业 + 外汇与套保 + 近45天）→ 5 维打分 → 写商机表
- **llm_review**：对单条商机调大模型 → 输出 ReviewResult；无 key 时返回"跳过"标记
- **build_queue**：按日期聚合商机、按分数排序 → 队列快照
- **render_web**：快照 → 静态 HTML（纯前端 JS 读内嵌 JSON，零服务依赖）
- **store**：统一 CRUD + 生命周期状态机（new→verifying→contacted / invalid）

---

## 4. 核心接口 / API

### Pi 扩展工具（Pi 视角）

| 工具 | 入参 | 出参 |
|------|------|------|
| `crawl_cninfo` | `days`(默认45), `keywords[]`, `region`(默认广东) | `{announcements: Announcement[], source: "live"\|"sample"}` |
| `rule_screen` | `region`, `days` | `{opportunities: Opportunity[], rule_hits: string[]}` |
| `review_opportunity` | `opportunity_id` | `{review: ReviewResult, skipped: bool}` |
| `build_daily_queue` | `date`(默认今天) | `{queue: DailyQueue}` |
| `render_web` | `queue` | `{html_path}` |
| `claim_opportunity` | `opportunity_id`, `owner` | `{lifecycle: "verifying"\|"contacted"}` |
| `mark_invalid` | `opportunity_id`, `reason` | `{lifecycle: "invalid"}` |

### Python 引擎 CLI（工具→引擎调用约定）

统一约定：`python engines/<name>.py --input <path> --out <path>`，JSON 走文件，stdout 只打印状态。
示例：`rule_screen.py --input data/crawled/2026-08-04.json --out data/db/`

---

## 5. 关键数据结构（JSON）

```jsonc
// Announcement
{ "id": "cninfo_20260731_001", "stock_code": "605499",
  "stock_name": "东鹏饮料", "title": "关于开展外汇衍生品交易的公告",
  "url": "...", "publish_date": "2026-07-31", "source": "巨潮资讯",
  "region_hint": "深圳", "keywords_hit": ["外汇与套保"] }

// Opportunity
{ "id": "opp_001", "announcement_id": "cninfo_20260731_001",
  "company_name": "东鹏饮料(集团)股份有限公司", "city": "深圳",
  "tags": ["境外市场", "币种待核实", "外汇与套保"],
  "trigger_event": "巨潮公告：关于开展外汇衍生品交易的公告",
  "rule_hits": ["广东企业", "外汇与套保", "近45天官方公告"],
  "score": 77,
  "score_breakdown": { "event_credibility": 95, "capital_scale": 48,
    "timeliness": 95, "coverage": 55, "info_completeness": 63 },
  "lifecycle": "new", "owner": null, "created_date": "2026-08-04" }

// ReviewResult
{ "opportunity_id": "opp_001",
  "evidence_summary": { "doc_title": "...", "source": "巨潮资讯·公司公告", "publish_time": "2026-07-31" },
  "known_facts": ["证券代码: 605499", "广东属地依据: ...", "命中事件: 外汇与套保"],
  "unknown_facts": ["跨境资金金额及付款节奏", "计价币种与汇率敞口", "现有结算银行及授信", "联系人/触达窗口"],
  "suggested_questions": ["当前主要外币敞口来自哪些业务和币种？", "现有套保比例与期限安排？", "是否需要远期/掉期/期权稳定人民币成本？"],
  "reviewed_score": null, "skipped": false }

// DailyQueue
{ "date": "2026-08-04", "generated_at": "2026-08-04T09:00:00",
  "items": [ { "rank": 1, "opportunity_id": "opp_001", "company_name": "...",
    "city": "深圳", "title": "...", "tags": ["..."], "score": 77 } ] }
```

---

## 6. 主链路时序

```
用户在 Pi 终端输入："跑一遍今日企业外汇需求商机雷达"
Pi Agent 读取 workflow skill，依次调用：
  1. crawl_cninfo(days=45, keywords=[外汇套保,远期结售汇,...], region=广东)
       → 公告写 data/crawled/；抓取失败 → 用 data/sample/（降级）
  2. rule_screen(region=广东, days=45)
       → 命中商机 + 5维打分 → 写 SQLite opportunities
  3. 循环 review_opportunity(每条商机)
       → LLM 复核（无 key → skipped，纯规则分）
  4. build_daily_queue(date=今天)
       → data/queue_snapshots/YYYY-MM-DD.json
  5. render_web(queue)
       → web/index.html
  6. Pi 汇报："今日队列 N 条，网页已生成：web/index.html"
交易员打开 web/index.html 查看队列/详情；
后续 claim_opportunity / mark_invalid 更新生命周期，重新 render_web 刷新页面。
```

---

## 7. 权限、隔离和边界

- **权限**：Pi 无内置权限系统，以当前用户权限运行（Pi README 明示）→ 扩展工具只执行白名单 Python 引擎命令，参数转义，禁止任意 shell
- **网络隔离**：爬虫只允许访问巨潮官方域名；LLM 只走配置的 provider
- **数据边界**：全部数据落 `data/`（SQLite+JSON），不外传；演示网页为静态文件，无服务端
- **行内系统边界**：【边界不稳】GC 台账/我行覆盖度无行内接口 → 本期 `coverage` 分用"配置占位规则"并标注，不做真实查询
- **LLM 依赖**：【待定】导师提供的 API 未到手 → `config.yaml` 支持 openai 兼容 provider；无 key 时复核降级跳过，主链路不断
- **抓取依赖**：【边界不稳】巨潮真实接口未验证 → 抓取失败自动回退 `data/sample/`
- **定时运行**：【待定】Pi 是交互式 CLI，"每日自动跑"本期不承诺，演示以现场一句话跑通为主
- **多用户**：【待定】不做登录鉴权，`owner` 仅记录字符串（演示用）