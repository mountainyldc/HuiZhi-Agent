# 自由提问与分析 —— 拓展计划（v0.1，仅计划，未实施）

> 目标：让用户可以直接对 Pi Agent 自由提问、分析商机数据。
> 状态：**计划中**，本文件只描述设计，不包含实现。
> 形态：**形态 A —— 在 pi-web 里直接问 Pi**（利用现有对话能力 + 新增工具，零 UI 成本）。

---

## 1. 总体思路

商机雷达现有能力是「抓取 → 筛选 → 评分 → 队列 → 页面」，产出是**列表**。
本拓展把它升级为「列表 + 可问答」：Pi 新增一组「查数 / 统计 / 分析」工具，
用户用自然语言提问，Pi 调用工具拿数据，再（按需）让 DeepSeek 生成带依据的分析。

新增 5 个 Pi 工具（按实施顺序）：

| 优先级 | 工具 | 一句话能力 |
|---|---|---|
| P0 | `query_opportunities` | 结构化查商机（公司/城市/评分/标签/日期过滤） |
| P0 | `queue_stats` | 队列统计（城市/标签/评分/公告类型分布） |
| P1 | `fetch_announcement_body` | 补抓公告正文（巨潮 PDF → 文本） |
| P1 | `ask_insights` | 通用分析问答（检索上下文 + DeepSeek 生成结论） |
| P2 | `analyze_company` | 单公司深度分析（正文+舆情聚合 → 敞口/建议） |

---

## 2. P0 — 结构化查询 `query_opportunities`

**做什么**：按条件过滤商机，返回结构化列表。回答：
- 「深圳最近有哪些外汇套保公告？」
- 「评分 70 以上的有几家？」
- 「标签含『境外投资/子公司』的公司」

**参数**（均可选，缺省返回全部）：

| 参数 | 类型 | 说明 |
|---|---|---|
| company | string | 公司名模糊匹配 |
| city | string | 城市（深圳/广州/…） |
| min_score / max_score | int | 评分区间 |
| tag | string | 标签过滤（外汇与套保/境外投资/舆情…） |
| lifecycle | string | new / verifying / contacted / invalid |
| days | int | 按公告日期最近 N 天 |
| limit | int | 返回条数上限（默认 20） |

**返回**：`[{company, city, score, tags, trigger_event, publish_date, evidence_url, opportunity_id}]`
按 score 降序。**实现位置**：新增 `engines/query_engine.py`，基于 `store.list_opportunities()` + `announcements` 联查，不直接改 SQLite 表结构。

---

## 3. P0 — 队列统计 `queue_stats`

**做什么**：对当前队列做聚合统计。回答：
- 「今天 119 条里多少是套保制度类？」
- 「深圳 vs 广州的分布？」

**参数**：`group_by`（city / tag / score_band / announcement_type / source），可加 `top` 取前 N。

**返回**：
```json
{ "group_by": "city", "total": 119,
  "groups": [{"key": "深圳", "count": 52, "avg_score": 71}, ...] }
```
**实现位置**：同一 `query_engine.py`，用 SQL GROUP BY / Python 聚合。

---

## 4. P1 — 公告正文抓取 `fetch_announcement_body`

**背景（当前最大数据缺口）**：`crawl_cninfo.py` 存公告只保存了标题 + URL + 元数据，
`raw_text = None`。所有「深度分析」都缺公告正文，只能看标题。

**做什么**：给定 `announcement_id`（或 URL），下载巨潮 PDF，解析成文本，
写回 `announcements.raw_text` 并缓存到 `data/bodies/<id>.txt`。

**要点**：
- 解析用 `pdfplumber`（或先试 `pypdf`，纯文本优先）。
- 容错：下载失败 / 非文本 PDF / 超时 → 返回 `{status:"skipped", reason}`，不阻断主流程。
- 缓存命中不重复下载；`llm_review` 可复用正文提升复核质量。
- 触发方式：Pi 工具 `fetch_announcement_body`（可传 `--all-top 30` 批量补抓高分商机）。

---

## 5. P1 — 通用分析问答 `ask_insights`

**做什么**：回答开放问题，例如：
- 「为什么东鹏饮料排第一？」
- 「这批商机集中反映了什么趋势？」
- 「哪些商机最值得优先跟进？」

**流程（两段式）**：
1. **检索**：把用户问题交给一个小路由（关键词/LLM 分类），转成结构化查询
   （`query_opportunities` / `queue_stats` 的调用），拿到上下文数据；
2. **生成**：把「问题 + 检索结果 + 相关公告标题/正文摘要」拼进 prompt，
   调 DeepSeek 生成带依据的结论，要求每条结论标注来源（公告/舆情/URL）。

**实现位置**：`engines/ask_engine.py`（复用 `llm_review.py` 的 API 通道），
Pi 注册工具 `ask_insights(question, scope?)`。

**防幻觉**：系统提示强约束「只能基于给定上下文回答；无依据时明确说『数据不足』」；
返回格式 `{answer, evidence: [来源列表], insufficient: bool}`。

---

## 6. P2 — 单公司深度分析 `analyze_company`

**做什么**：对指定公司做一次「外汇需求体检」。回答：
- 「帮我分析比亚迪的外汇需求。」
- 「美的集团套保现状和合作机会？」

**输入**：`company`（必填），可加 `focus`（敞口/合作银行/营销建议）。

**流程**：
1. 查该公司全部商机 + 公告正文（无正文时自动调 `fetch_announcement_body` 补抓）；
2. 抓该公司近期舆情（复用 `sina_news` / `eastmoney_news` 或按其代码定向搜索）；
3. DeepSeek 输出结构化报告：外汇风险敞口 / 套保现状 / 结算币种 / 合作机会 / 建议沟通问题 / 信息来源。

**返回**：Markdown 报告（存 `data/analyses/<company>-<date>.md`，可复用、可留档）。

---

## 7. 数据与接口改动汇总

| 文件 | 改动 |
|---|---|
| `engines/query_engine.py`（新增） | `query_opportunities` / `queue_stats` 实现 |
| `engines/ask_engine.py`（新增） | `ask_insights` / `analyze_company` 编排 + LLM 生成 |
| `engines/crawl_cninfo.py` | `fetch_announcement_body`：PDF 下载 + 解析 + 缓存 |
| `engines/store.py` | `get_announcement_body` / 缓存读写（表结构可不动） |
| `engines/llm_review.py` | 复用其 DeepSeek 客户端，抽象出公共 `llm_call` |
| `extensions/fx-lead-radar.ts` | 注册 4 个新工具（9 → 13 个） |
| `skills/workflow/SKILL.md` | 增加「自由提问」使用说明与示例问题 |

**不做**：不改现有队列/评分/页面逻辑；不新增网页问答 UI（形态 B 留作后续）。

---

## 8. 实施顺序（后续按此执行，当前不实施）

1. **第 1 步（约 1 天）**：`query_engine.py`（query + stats）+ Pi 注册两个 P0 工具 + 全局同步。
2. **第 2 步（约 1-2 天）**：`fetch_announcement_body` 正文抓取 + `ask_insights` 两段式问答。
3. **第 3 步（约 1 天）**：`analyze_company` 单公司深度报告。
4. **第 4 步**：与导师演示「自由提问」，收集问题类型，反哺路由与 prompt。

---

## 9. 风险与边界

- **LLM 超时/成本**：DeepSeek 复核曾超时跳过；分析请求设 60s 超时，失败降级为
  「规则分 + 检索结果」（不带 LLM 结论），不阻塞。
- **PDF 解析失败**：巨潮部分公告为扫描件/加密，容错跳过并标注。
- **幻觉**：所有 LLM 结论必须带来源；`ask_insights` 无依据时输出「数据不足」。
- **历史趋势暂不支持**：每日快照归档（趋势分析）本轮未选，留作后续 P2.5。
- **数据量**：正文抓取 119 条全量耗时较长，默认只抓 Top 30 高分商机。
- **权限**：新工具均走本地 `serve.py`/Pi 会话，不对外网暴露。
