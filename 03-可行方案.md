# 03-可行方案（结合 Pi Agent 的企业外汇需求商机雷达）

> 更新日期：2026-08-04
> 前提：基于 02-问题定义.md 已确认的需求；优先考虑稳定交付，不做最炫。

---

## 〇、参考项目价值评估

### Horizon（Thysrael/Horizon，★8.6k，MIT，活跃）
- **可借鉴**：聚合器→过滤→AI评分(0-10)→日报/推送 的 pipeline 结构；MCP server 思路（把数据源做成 MCP 工具）；定时任务+飞书推送模式
- **不能直接搬**：它是"资讯日报"（内容质量评分），没有商机识别规则、客户维度、认领/线索管理、行内数据；中英双语我们不需要
- **定位**：架构模式参考价值高，代码复用价值低（需要重写业务规则）

### 其他项目改造价值
| 项目 | 可借鉴点 |
|------|---------|
| SeekMoney-ai (★119) | 商机评分体系：需求强度/市场规模/竞争度 → 映射我们的 5 维商机分 |
| Daniele-Cangi/Lead-Radar (★3) | scan→enrich→score 模块化后端架构 |
| compass-fx-pulse (★1) | 外汇域 Agent+RAG+MCP 组合、外汇特征与提示词 |
| mrphatom/LeadsRadar (★2) | 队列+详情+跟进的 UI 交互 |
| bossai-radar-lite (★0) | 商机雷达"技能化"组织（MCP+Skills），与 Pi Agent 路线一致 |
| 交易类 QuantDinger/OpenAlice/Lumibot | 仅技术栈参考，方向不同不改造 |

---

## 方案 A：规则引擎为核心 + 轻量 Web，Pi Agent 做"AI 复核"模块 ★推荐

**1. 大致怎么做**
- 数据层：Python 抓巨潮公告（官方接口），失败可降级为内置样例数据（保证演示不断）
- 规则层：确定性规则初筛（广东企业 + 外汇与套保 + 近45天公告）+ 5 维规则打分（事件可信度/资金体量/时效性/我行覆盖度/信息完整度），分项可见、合计=总分，完全可解释
- AI 层：封装 Pi Agent Skill 做「大模型复核」——对命中商机生成证据摘要、已知/未知事实、建议沟通问题（对应截图"待大模型复核"）
- 展示层：轻量 Web（Flask/FastAPI + 简单前端），队列列表/详情/认领/标记无效，SQLite 存商机与状态
- 数据流：抓取 → 规则初筛 → Pi Agent 复核 → 入库 → 展示

**2. 主要优点**
- 每层可独立跑通，抓取失败有样例兜底，演示稳定性最高
- 评分可解释，被追问"分数怎么来的"能答
- Pi Agent 深度参与（复核 skill），汇报可讲"规则引擎+Pi Agent 双引擎"

**3. 主要风险**
- 巨潮接口变更/限流 → 样例兜底已对冲
- "我行覆盖度"无行内数据 → 用占位规则并明确标注
- 大模型复核无 API → 降级为纯规则版，演示脚本仍完整

**4. 实现复杂度**：低-中（约 2-3 周）
**5. 可测试性**：高（规则/打分单测、页面截图、样例数据端到端可复现）

---

## 方案 B：Pi Agent 全自动端到端 pipeline

**1. 大致怎么做**
- 用 Pi Agent 编排完整 workflow：巨潮抓取 skill → 规则过滤 skill → LLM 复核 skill → 每日商机队列生成 → 飞书/企微推送 → Web 展示
- 所有能力封装成 Pi Agent skills，汇报演示"一句话让 agent 跑完全流程"

**2. 主要优点**
- 最能展示 Pi Agent 的 agent 编排价值，演示最"炫"
- 最贴合导师"每日队列 + 待大模型复核"的自动化愿景

**3. 主要风险**
- 链路长、外部依赖多（巨潮接口+LLM API+推送 webhook），任一环节挂=演示翻车
- 调试成本高，稳定性难保证，与"稳定交付"冲突

**4. 实现复杂度**：高
**5. 可测试性**：中（需 mock 各外部依赖，端到端难稳定复现）

---

## 方案 C：完整产品版（复刻导师截图前端 + 线索管理）

**1. 大致怎么做**
- 前端尽量还原导师截图（队列/详情/生命周期/评分条/认领按钮）
- 后端 SQLite：商机表 + 线索状态机（新发现→待核实→已联系）+ 认领/标记无效 + 线索管理页
- 数据：巨潮真实抓取 + 5 维规则评分 + Pi Agent 复核

**2. 主要优点**
- 交付物最像导师预期，汇报"产品完成度高"，闭环完整

**3. 主要风险**
- 工作量最大，前端细节多，周期内容易做不完或粗糙
- 依赖最多，问题暴露面大
- 拿不到行内数据时"我行覆盖度"照样占位，UI 再全也显假

**4. 实现复杂度**：高
**5. 可测试性**：中（页面可测，全栈联调成本高）

---

## 推荐：方案 A

**为什么选 A**：用户明确"优先稳定交付，不做最炫"。A 的核心（规则+评分+样例兜底+可解释）实习期内确定性最高、演示翻车点最少；Pi Agent 仍深度参与（复核 skill），汇报能完整讲"规则引擎 + Pi Agent"的双引擎故事。

**为什么不选 B**：链路过长、外部依赖多，演示一旦外部接口抽风即翻车，稳定性不可控。
**为什么不选 C**：工作量与依赖最多，UI 完整但核心数据仍可能占位，性价比低。

**演进路径（A 起步可平滑加量）**：
```
A（规则+样例+复核）→ 接真实巨潮抓取 → 加 B 的自动编排/推送 → 加 C 的完整线索管理 UI
```
每步都在已跑通的基础上增量，风险可控。